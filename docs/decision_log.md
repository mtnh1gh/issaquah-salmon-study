# Decision log

| ID | Date | Decision | Rationale | Effect | Status |
|---|---|---|---|---|---|
| D-001 | 2026-07-19 | Use USGS station 12121600 for discharge only. | The official station page lists discharge and gage height; its available parameter record does not include water temperature. | King County temperature grab samples are now the candidate temperature source; approve their station and aggregation rule before creating a temperature feature. | Open |
| D-002 | 2026-07-19 | Use NRCS Stampede Pass SNOTEL station 788 for the initial SWE proxy. | NRCS current station listing identifies Stampede Pass as station 788, active since 1979. | Replace the draft code/config reference to station 769 after protocol approval. | Open |
| D-003 | 2026-07-19 | Treat current WDFW PDFs as supporting material, not the analytical response dataset. | They are not yet a complete, machine-readable, definition-consistent annual series. | The machine-readable WDFW event export is now the raw candidate response source; use annual PDFs only for reconciliation. | Open |
| D-004 | 2026-07-19 | Cache raw public downloads with date and SHA-256 before transformation. | Supports reproducibility and detection of source revision. | Environmental source adapters must read these snapshots rather than live endpoints during analysis. | Accepted |
| D-005 | 2026-07-19 | RMIS release API cannot be used anonymously. | The documented `GET /release` endpoint returned HTTP 401 with `No authorization token`; the published OpenAPI schema lists API-key/JWT security. | Obtain authorized RMIS access or a FISH/RMIS export before release-data collection can complete. | Open |
| D-006 | 2026-07-19 | Use the WDFW public Hatchery Adult Salmon Returns event dataset as the raw candidate response source for Issaquah Chinook and Coho. | The official State of Washington dataset provides a machine-readable, filtered 1995-2025 event series with complete adult and jack fields for both species. | Preserve the raw event records; approve one non-overlapping annual count/return definition before aggregation or modeling. | Open |
| D-007 | 2026-07-19 | Use King County Water Quality temperature grab samples as the candidate water-temperature source. | The official dataset has long coverage at two relevant Issaquah Creek sites, including one upstream of the hatchery. | Choose a single station and sampling-aware aggregation after reviewing coverage by analysis year; do not represent the grab samples as a continuous temperature record. | Open |
| D-008 | 2026-07-26 | Use the King County `TOPO_BASIN_KC_AREA` Issaquah Creek feature as the analytical watershed boundary. | It is the county-maintained terrain-derived basin layer, identifies Issaquah Creek within WRIA 8, and provides one polygon with a provider-reported area of 58.351 square miles. | Use this polygon to select and clip impervious-surface data. Keep the raw boundary out of redistributed releases unless King County grants permission. | Accepted |
| D-009 | 2026-07-26 | Define annual returns as the sum of WDFW `Trap Estimate` adult counts by return year and species; retain jacks separately. | Trap Estimate rows reproduce WDFW published origin-specific totals. Other event categories describe subsequent handling and would double-count the same fish if included. | The response series contains 58 species-year rows for 1997–2025. Adult totals are the primary response; jacks remain available for sensitivity analysis. | Accepted |
| D-010 | 2026-07-26 | Use King County station 0631 at SE 56th St for a June–September grab-sample temperature index. | It has at least four warm-season samples in every 1997–2025 response year; the upstream station has a 2009–2012 gap. | Use the June–September arithmetic mean with a four-sample minimum. Describe it only as a grab-sample index. | Accepted |
| D-011 | 2026-07-26 | Replace the planned legacy-NLCD interpolation with Annual NLCD Collection 1.2 Fractional Impervious Surface values. | The current official product provides annual 30 m values for 1985–2025 and measures pixel-scale percent artificial surface. | Extract annual basin means directly after a raster-capable runtime or official clipped download is available; do not interpolate the old nine epochs. | Accepted |
| D-012 | 2026-07-26 | Pass the Phase 2 cleaning/master-data gate under analysis protocol v1.0. | A cached-input rebuild produced byte-identical outputs and all 14 critical validation checks passed. The master has 58 species-year rows with complete core features, explicit cohort alignment, and flagged—not filled—blocked fields. | Proceed to exploratory analysis using observational-association language; Coho lag 2 and Chinook lag 4 are primary proxies, with Chinook lags 3 and 5 required for sensitivity. | Accepted |
| D-013 | 2026-07-26 | Pass the Phase 3 exploratory-analysis gate. | The reproducible EDA completed all pre-specified trend, correlation, missingness, and lag-sensitivity checks. No return-predictor correlation passed BH adjustment, and Chinook cohort-flow results were not stable across lags. | Proceed to parsimonious Phase 4 modeling with time-aware validation; treat results as exploratory associations and retain the documented blocked fields as exclusions. | Accepted |
| D-014 | 2026-07-27 | Pass the Phase 4 statistical-modeling gate. | Six interpretable association models and four naive comparators were evaluated with expanding-window validation. Three Coho candidates beat both naive baselines; no Chinook candidate did. | Retain the three Coho candidates for Phase 5 uncertainty/scenario review only; do not treat any model as causal or automatically eligible for 2040 projections. | Accepted |
| D-015 | 2026-07-27 | Complete the Phase 5 uncertainty review without approving a final scenario model. | Bootstrap intervals around 17 rolling-origin errors were wide and empirical interval coverage was 88.2% for each retained Coho candidate; the sample is too small to certify nominal 95% calibration. | Carry all three candidates into conditional Phase 6 scenario illustrations only, with explicit uncertainty bounds and no causal or policy claims. | Accepted |
| D-016 | 2026-07-27 | Limit Phase 6 projections to conditional Coho illustrations based on observed predictor quantiles. | No Chinook candidate passed the Phase 4 baseline gate, and no authoritative future trajectories are available for all predictors. | Report 10th/50th/90th observed-quantile sensitivity through 2040 as illustrative only; do not call these forecasts or policy scenarios. | Accepted |
| D-017 | 2026-07-27 | Complete the final awareness-report package with release caveats. | The full pipeline, validation reports, scenario limitations, and unresolved data blockers are documented; no domain-partner review has yet occurred. | Treat the package as a reproducible draft for stakeholder/domain review, not a final management decision document. | Accepted |
| D-018 | 2026-07-29 | Formally adopt Bronze/Silver/Gold directory layering in place of the ad-hoc `data/raw`/`data/processed` split, retiring the empty placeholder folders from the project's initial setup. | The original project setup (commit `16e7ebd`) staged empty `data/bronze`/`data/silver`/`data/gold` stubs as the intended architecture; the pipeline that was actually built already matches that design in substance (checksummed source snapshots -> cleaned/conformed annual tables -> joined model-ready master), just under different names. Formalizing the naming removes the stale stubs and makes the existing design explicit. | `data/raw/<source>/<date>/...` moves to `data/bronze/...` (raw immutable snapshots); `data/processed/wdfw_issaquah_annual_returns.csv` and `issaquah_annual_environment.csv` move to `data/silver/...` (cleaned, source-conformed tables); `data/processed/issaquah_creek_master.csv` moves to `data/gold/issaquah_creek_master.csv` (joined, cohort-aligned, model-ready table). All build/run/validate script defaults and doc path references are updated to match. This is a path/organization-only change; no data values are altered. | Accepted |
| D-019 | 2026-07-29 | Cache two new ocean-condition indices (NPGO, ONI) and three supporting harvest/pinniped-predation documents as acquisition candidates; re-confirm RMIS has no anonymous access path. | Requested review of what additional data could strengthen the analysis. RMIS's public site (`rmis-files`, `rmis-queries`, `find-tag`) and the Issaquah hatchery's own public page were both checked directly; neither offers an annual release series without authorized RMIS access, so R-003/D-005 stand unchanged. NPGO and ONI are literature-supported ocean-productivity/ENSO proxies distinct from PDO; the harvest and pinniped-predation documents address the omitted-variable concern in R-009/R-012 (harvest and Ballard Locks predation losses are not represented in any current feature). | Raw files cached under `data/bronze/npgo/`, `data/bronze/noaa_oni/`, `data/bronze/pinniped_predation/`, and `data/bronze/harvest_management/` (2026-07-29 snapshots) and registered in `docs/source_register.csv`/`docs/data_inventory.csv`. Candidate feature rows F-032/F-033 added to `docs/feature_registry.csv` with `included_in_model: no` pending an aggregation/lag decision. The three PDF documents remain supporting/qualitative only -- no PDF-text-extraction tool is available in this environment, so no numeric series was pulled from them. None of this changes any Phase 3-6 script, model, or reported result. | Accepted |
| D-020 | 2026-07-29 | Define the NPGO and ONI aggregation and marine-window rule, promote F-032/F-033 to active pre-specified predictors, and add them as Phase 3/4 candidate features under protocol v1.1. | NPGO (monthly, complete 12 months/year over the study period) and ONI (published as overlapping 3-month seasons) are both literature-supported ocean-condition proxies distinct from PDO; testing them the same way PDO was tested keeps the comparison apples-to-apples. Mirroring the existing `pdo_annual_mean` -> `marine_pdo_mean` pattern reuses an already-reviewed aggregation logic rather than inventing a new one. | Silver layer: `npgo_annual_mean` = mean of the 12 monthly NPGO values in the calendar year; `oni_annual_mean` = mean of the (overlapping) seasonal ONI/ANOM values tagged to the calendar year. Both required to have full 12-value coverage per year or the build fails loudly (`src/build_annual_environment.ps1`). Gold layer: `marine_npgo_mean` and `marine_oni_mean` = mean of the annual index from `cohort_environment_year + 1` through `return_year - 1`, identical window to `marine_pdo_mean` (`src/build_analysis_table.ps1`). Phase 3 (`src/run_eda.py`): both added to the trend table and the correlation/lag battery. Phase 4 (`src/run_phase4_models.py`): two new candidate models added alongside the three already-decided ones (not modifying them) -- `ocean_index_ols` (`marine_pdo_mean`, `marine_npgo_mean`, `marine_oni_mean` only) and `all_environment_ocean_ridge` (the five original `all_environment_ridge` features plus both new indices). `docs/analysis_protocol.md` bumped to v1.1. All counts in `src/validate_phase3.py`/`validate_phase4.py` updated to match the larger pre-specified test set before any result was inspected. | Accepted |
| D-021 | 2026-08-23 | Extend both temperature proxies to 1995; designate T2 biological-window mean as the Phase 7 primary thermal variable, T1 window mean as model sensitivity, and threshold/nonmean metrics as exploratory; freeze the exact Phase 7 protocol. | Coho A5 needs exposure year 1995 for the 1997 return cohort. T2 performs similarly to T1 while excluding flow, avoiding mathematical coupling in flow-temperature and combined analyses. Modeled threshold counts are not observed regulatory exceedances. | Acquire and checksum 1994-12-03 through 2025 predictor inputs; generate complete 1995-2025 T1/T2 series, matched A1-A5 exposure windows, and detailed audits for both models. Apply the predictor/outcome/lag/test definitions in `docs/phase7_hypothesis_analysis_protocol.md`; any change requires a dated amendment before rerunning affected Phase 7 results. | Accepted |
